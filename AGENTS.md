# Agent Guidelines & Workflow Rules

## Git Push Protocol
- Always require explicit user instruction before pushing to remote repositories.
- **When the user instructs to push, push directly to the `main` branch across the target remote repositories (e.g. `git push <remote> <local_branch>:main`).**
